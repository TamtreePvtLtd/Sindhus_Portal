export enum paths {
  ROOT = "/",
  HOME = "/home",
  SPECIALS = "/specials",
  SNACKS = "/snacks",
   MENUS = "/menus",
  CATERING = "/catering",
  DAILYMENU = "/dailyMenu",
  PRODUCTDETAIL = "/detail/:productId",
  PRODUCTSBYCATEGORY = "/productsByCategory/:menuId",
}
